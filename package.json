{
  "private": true,
  "scripts": {
    "dev": "turbo run dev --parallel --turbo",
    "build": "turbo run build"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^1.6.3"
  }
}
